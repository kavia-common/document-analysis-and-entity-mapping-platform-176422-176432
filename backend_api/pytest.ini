[pytest]
minversion = 8.0
addopts = -q --maxfail=1 --disable-warnings --cov=src --cov-report=term-missing
testpaths = 
    tests
asyncio_mode = auto
markers =
    pg_required: Tests that require a running PostgreSQL instance
    integration: Integration tests
